export const getImagesRemotePath = (fileName) => `images/${fileName}`;

export const getIconsRemotePath = (fileName) => `icons/${fileName}`;

export const getVideosRemotePath = (fileName) => `videos/${fileName}`;

export const getAppsRemotePath = (fileName) => `apps/${fileName}`;
