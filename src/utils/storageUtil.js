const getImagesRemotePath = (fileName) => `images/${fileName}`;

const getIconsRemotePath = (fileName) => `icons/${fileName}`;

const getVideosRemotePath = (fileName) => `videos/${fileName}`;

const getAppsRemotePath = (fileName) => `apps/${fileName}`;

module.exports = {
  getImagesRemotePath,
  getIconsRemotePath,
  getVideosRemotePath,
  getAppsRemotePath,
};
